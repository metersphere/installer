version: "2.1"

volumes:
  ms-conf:
    driver_opts:
      type: none
      device: ${base_dir}/metersphere/conf
      o: bind
  ms-logs:
    driver_opts:
      type: none
      device: ${base_dir}/metersphere/logs
      o: bind

networks:
  ms-network:
